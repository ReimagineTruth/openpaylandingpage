/** Stub for preview gallery — Regulatory modal is not required on the marketing site. */
export default function RegulatoryStatusModal({
  open: _open,
  onOpenChange: _onOpenChange,
}: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}) {
  return null;
}
